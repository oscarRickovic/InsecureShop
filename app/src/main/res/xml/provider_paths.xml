<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App-specific files directory -->
    <files-path name="app_files" path="files/" />
    
    <!-- App cache directory -->
    <cache-path name="app_cache" path="cache/" />
    
    <!-- If you need to access files in app-specific external storage -->
    <external-files-path name="external_files" path="." />
    
    <!-- Only if you need to share specific media from external storage -->
    <external-path name="shared_images" path="Pictures/InsecureShop/" />
</paths>